import { Canvas, Meta, Story } from '@storybook/addon-docs';

import Button from '../MdxButton';

<Meta title="atoms/使用MDX文档" component={Button} />

export const Template = (args) => <Button {...args} />;


# Button

text text text text text text text text text text text text text text text text text text text text 

<Story
  name="red"
  args={{
    color: 'red',
    children:'button'
  }}>
  {Template.bind({})}
</Story>


text text text text text text text text text text text text text text text text text text text text 

<Story
  name="blue"
  args={{
    color: 'blue',
    children:'button'
  }}>
  {Template.bind({})}
</Story>